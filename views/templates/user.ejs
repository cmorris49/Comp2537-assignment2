<li class="list-group-item d-flex justify-content-between align-items-center">
  <span>
    <% if (user.name === currentName) { %>
      ★ 
    <% } %>
    <%= user.name %> (<%= user.user_type %>)
  </span>
  <% if (user.user_type === 'user') { %>
    <a href="/admin/promote/<%= user.name %>" class="btn btn-sm btn-outline-success">
      Promote
    </a>
  <% } else { %>
    <a href="/admin/demote/<%= user.name %>" class="btn btn-sm btn-outline-warning">
      Demote
    </a>
  <% } %>
</li>
